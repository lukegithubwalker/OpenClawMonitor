name: clawscope
description: OpenClaw AI monitoring dashboard
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.68.0
  http: ^1.2.0
  web_socket_channel: ^2.4.0

flutter:
  uses-material-design: true
